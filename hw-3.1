'tmp <- "\"cat\" \"at\" \"t\" "'
cat(tmp)

for (n in 1:3){
  x <- c('c',rep('a',n),'t')
  cat(x)
}

cat(tolower('D'),'o','g')
cat(toupper('d'),'o','g')
cat('d','o',toupper('g'))

round(7/3, digit=3)
